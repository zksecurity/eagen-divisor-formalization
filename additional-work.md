# Additional Work Prompt

```text
You are in /Users/rot256/src/divisors. Start this work from a new branch before editing, for example:

  git switch -c work/divisor-class-bridge

Consult Codex from the shell when useful for a second opinion or subproblem
breakdown. Example command pattern:

  echo "hello" | codex exec --skip-git-repo-check

Work on the divisor-class bridge axiom:

  Divisor.CoordRingElt.divisorClass_isPrincipal_of_not_const_unit

Goal:
Investigate and, if feasible, mechanize this axiom without introducing any new axioms, `sorry`, or `admit`.

Primary files:
- Divisor/OrdP/LocalRing.lean
- Divisor/OrdP/Uniformizer.lean
- Divisor/OrdP/PrincipalClass.lean
- Divisor/HasPrincipalDivisor.lean
- Divisor/Axioms/AxiomExistsDivisorMultiplicity.lean

Docs:
- docs/isprincipal-mathlib-route.md
- axioms/divisorClass_isPrincipal.md

Current state:
- `ordAt` is already defined in `Divisor/OrdP/Uniformizer.lean`.
- Support, coverage, degree bound, and split accounting for `ordAt` are already proved in `Divisor/OrdP/LocalRing.lean`.
- The constant-unit case of `divisorClass_isPrincipal` is already proved.
- The old existential multiplicity axiom is already discharged using `ordAt`.
- The remaining axiom is the concrete class-group bridge for non-constant-unit `D`.

Do not prove this by adding a generic "Abel's theorem" axiom. The missing content is the concrete local compatibility theorem:

  The project's recursive `ordAt E D P` equals the exponent/valuation of
  the coordinate-ring element represented by `D = a - bY` at the affine
  point ideal `XYIdeal x y`.

Suggested route:
1. Define or reuse the coordinate-ring element corresponding to
   `D.a - D.b * Y` in `E.toW.toAffine.CoordinateRing`.
2. Connect affine evaluation `D.eval x y = 0` with membership of that element in
   `CoordinateRing.XYIdeal x (C y)`.
3. Prove local order compatibility for the easy cases first:
   - constant unit, if not already enough;
   - non-vanishing at `P`;
   - simple non-2-torsion zero;
   - 2-torsion closed-form case if tractable.
4. Relate powers of `XYIdeal` or localization-at-prime valuations to the recursive `ordAt`.
5. Show the divisor class of `divisorOfD E D` is the class of a principal fractional ideal generated by the coordinate-ring element.
6. Use `ClassGroup.mk_eq_one_iff` / principal fractional ideal facts to conclude the class is trivial.

Relevant mathlib surface:
- `WeierstrassCurve.Affine.CoordinateRing`
- `WeierstrassCurve.Affine.CoordinateRing.mk`
- `WeierstrassCurve.Affine.CoordinateRing.XYIdeal`
- `WeierstrassCurve.Affine.CoordinateRing.XYIdeal'`
- `WeierstrassCurve.Affine.Point.toClass`
- `WeierstrassCurve.Affine.Point.toClass_eq_zero`
- `ClassGroup.mk_eq_one_iff`
- `FractionalIdeal`
- `Submodule.IsPrincipal`
- localization / ideal powers APIs if needed

Hard requirements:
- Do not introduce new axioms.
- Do not replace this axiom with a different opaque axiom.
- Do not leave `sorry` or `admit`.
- Do not include time estimates.
- If the full axiom is too large, land complete helper lemmas that move the proof forward.
- Keep theorem names stable for downstream consumers where practical.
- Keep edits scoped.

Useful intermediate deliverables:
- A lemma constructing the coordinate-ring element for `CoordRingElt`.
- A lemma showing evaluation zero iff membership in `XYIdeal`.
- A lemma for nonzero evaluation implying local order/exponent zero.
- A lemma matching `ordAt_twoTorsion` with the ideal/local-order formula.
- A lemma matching the non-2-torsion recursive branch with division by `X - C x₀`.
- A precise final theorem statement for the remaining blocker if full discharge is not completed.

Verification:
Run:
- lake env lean Tests/AxiomClosurePin.lean
- lake build Divisor
- git diff --check

Report:
1. Findings/blockers first.
2. Files changed.
3. Complete helper lemmas proved.
4. Whether `CoordRingElt.divisorClass_isPrincipal_of_not_const_unit` was removed, narrowed, or unchanged.
5. Exact remaining theorem statement if blocked.
```
